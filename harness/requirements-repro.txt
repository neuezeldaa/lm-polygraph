# =============================================================================
# Stage 1 reproducibility pin — Spilled Energy on lm-polygraph
#
# Target runtime: free Google Colab / Kaggle T4 (16 GB, CUDA 12.x, fp16, no FA2).
# lm-polygraph itself is installed from the pinned commit below (editable clone),
# which pulls its own requirements.txt; this file pins the versions that matter
# for reproducibility on top of that.
#
# lm-polygraph commit: efea882d810d07770e71d3a80e02416d09751435  (2026-08-04)
#   git clone https://github.com/IINemo/lm-polygraph
#   git -C lm-polygraph checkout efea882d810d07770e71d3a80e02416d09751435
#   pip install -e lm-polygraph
#   pip install -r lm-polygraph/requirements-repro.txt
#
# Install torch FIRST with the CUDA 12.4 wheel index (see notebooks/stage1_colab_t4.ipynb):
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# =============================================================================

torch==2.6.0
transformers==4.50.0
tokenizers>=0.21,<0.22
datasets==2.21.0            # <3.0: avoids the removal of script-based dataset loaders
accelerate==1.4.0
numpy==1.26.4              # <2.0 for broad compatibility with the pinned torch/transformers
hydra-core==1.3.2
omegaconf==2.3.0
sentencepiece==0.2.0
safetensors>=0.4.5
rouge-score==0.1.2         # AccuracyMetric path only needs stdlib, but repo imports these at module load
sacrebleu>=2.4.0
