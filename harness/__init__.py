"""Experiment harness for the Spilled Energy assignment.

A package so estimators here can be loaded by dotted path
(`- name: harness.pooled_baseline`) via lm-polygraph's FactoryEstimator.
Requires the repo root on PYTHONPATH; run_baselines.py sets that for the
polygraph_eval subprocess.
"""
